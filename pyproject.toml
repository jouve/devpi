[tool.poetry]
name = "devpi-docker"
version = "0.1.0"
description = ""
authors = []

[tool.poetry.dependencies]
python = "^3.10,>=3.10.4"
devpi-server = "*"
devpi-web = "*"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
